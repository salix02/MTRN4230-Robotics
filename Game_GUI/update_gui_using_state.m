function update_gui_using_state()
%% update_gui_using_state - This fucntion updates all the icons based on the games internal state, NOT on what the camera sees.
% %useful for debugging.
% inputs none
% outputs none
% uses globals to store changed state in the Game GUI
% author: Harry Dudley-Bestow
% Last modified: 08/11/2017

% global board_stat board_buttons deck
% for row = 1:9
%     for col = 1:9
%         board_buttons.grid(row,col).Icon = pick(squeeze(board_stat(row, col, :)));
%     end
% end
% 
% for i = 1:6
%     board_buttons.p1(i).Icon = pick(deck(1, i, :));
%     board_buttons.p2(i).Icon = pick(deck(2, i, :));
% end

end
